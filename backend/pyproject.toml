[project]
name = "crop-ocr-api"
version = "0.1.0"
description = "Memory-only HTTP API for OCR on browser-selected image crops"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.135,<1",
  "numpy>=2,<3",
  "paddleocr==3.4.0",
  "pillow>=12,<13",
  "uvicorn[standard]>=0.41,<1",
]
